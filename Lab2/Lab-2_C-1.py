# Lab-2_C-1

# create list containing duplicate course names
dupe_courses_list =["python", "python", "java", "java"]
print(dupe_courses_list, (len(dupe_courses_list)))

# convert to set and compare the lenght before and after.
dupe_courses_set = set(dupe_courses_list)
print(dupe_courses_set, (len(dupe_courses_set)))